import ReferralPage from "./ReferralPage";

export const metadata = {
  title: "Referral Program",
  description: "Invite friends to Polycogni Capital and earn rewards.",
};

export default function Page() {
  return <ReferralPage />;
}