import SwapPage from "./SwapPage";

export const metadata = {
  title: "Swap",
  description: "Swap assets within your Polycogni Capital account.",
};

export default function Page() {
  return <SwapPage />;
}